# Build a typed NixOS deployment closure with `kai machine server`.
environment server {
  packages: ["curl"]
}

machine server {
  environment: server
  system: "x86_64-linux"
  users: ["agent"]
  services: ["openssh"]
  backend nix {
    networking.hostName: "kai-server"
  }
}
