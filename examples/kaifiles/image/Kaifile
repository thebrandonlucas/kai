# Build a generic QCOW2 image with `kai image server`.
environment server {
  packages: ["curl"]
}

machine server {
  environment: server
  system: "x86_64-linux"
  users: ["agent"]
  services: ["openssh"]
}
