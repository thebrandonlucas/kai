# Build a generic QCOW2 image from the same machine definition.
environment server {
  packages: ["curl"]
}

machine agent {
  environment: server
  system: "x86_64-linux"
  users: ["agent"]
  services: ["openssh"]
}
