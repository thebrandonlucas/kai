# Build a generic QCOW2 image with `kai image agent`.
environment server {
  packages: ["curl"]
}

machine agent {
  environment: server
  system: "x86_64-linux"
  users: ["agent"]
  services: ["openssh"]
}
