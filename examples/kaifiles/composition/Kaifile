# Compose a machine image as a typed dependency without deploying it.
environment server {
  packages: ["curl"]
}

machine agent {
  environment: server
  system: "x86_64-linux"
  users: ["agent"]
  services: ["openssh"]
}
