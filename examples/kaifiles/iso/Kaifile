# Build a bootable NixOS ISO with `kai iso server`.
environment recovery {
  packages: ["curl"]
}

machine server {
  environment: recovery
  system: "x86_64-linux"
  users: ["admin"]
  services: ["openssh"]
}
