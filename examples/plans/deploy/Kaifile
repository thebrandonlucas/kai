# Deploy this NixOS machine with `kai deploy server`.
environment server {
  packages: ["curl"]
}

machine server {
  environment: server
  system: "x86_64-linux"
  users: ["operator"]
  services: ["openssh"]
  target: "root@server.example.com"
}
