## The platform's API modules as a package, for Roc code that cannot import a
## platform: kai itself and the `ir` package. Both roots share these files, so
## a platform value is also a value of the package's type.
package [Layout, Sexpr, Value] {}
